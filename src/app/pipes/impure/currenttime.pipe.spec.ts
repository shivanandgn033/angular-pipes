import { CurrenttimePipe } from './currenttime.pipe';

describe('CurrenttimePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenttimePipe();
    expect(pipe).toBeTruthy();
  });
});
